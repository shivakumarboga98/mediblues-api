# ========================================
# Mediblues Backend Environment Variables
# ========================================

# NODE Environment
NODE_ENV=production

# ========================================
# Frontend Configuration (CORS)
# ========================================
# The domain where your frontend is hosted (for CORS policy)
# Development: http://localhost:5173
# Production: https://your-app.amplifyapp.com or your custom domain
FRONTEND_URL=https://your-frontend-domain.amplifyapp.com

# ========================================
# AWS Configuration
# ========================================
AWS_REGION=eu-north-1
S3_BUCKET_NAME=mediblues-assets

# Optional: If using CloudFront distribution for S3
# Format: d123456.cloudfront.net (without https://)
# Leave empty if using direct S3 URLs
CLOUDFRONT_DOMAIN=

# AWS Credentials (for local development only - NOT needed in AWS Lambda)
# Lambda uses IAM role instead of credentials
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# ========================================
# RDS MySQL Database Configuration
# ========================================
# Get RDS Endpoint from AWS Console: RDS > Databases > your-db > Connectivity & security
DB_HOST=mediblues-mysql.xxxxx.eu-north-1.rds.amazonaws.com
DB_PORT=3306
DB_NAME=mediblues_db
DB_USER=mediblues_user
DB_PASSWORD=your-strong-database-password

# Database Connection Pool Settings (for Lambda)
DB_POOL_MIN=1
DB_POOL_MAX=2
DB_WAIT_FOR_CONNECTIONS=true
DB_CONNECTION_LIMIT=2
DB_QUEUE_LIMIT=0

# ========================================
# JWT Configuration
# ========================================
# CRITICAL: Change these values in production!
# JWT_SECRET: Random string of min 32 characters
# Generate: openssl rand -base64 32
JWT_SECRET=your-random-secret-key-minimum-32-characters-long-change-in-production

# Admin Credentials (change password after first login)
ADMIN_EMAIL=admin@mediblues.com
ADMIN_PASSWORD=Admin@123

# ========================================
# Serverless Framework
# ========================================
STAGE=prod

# ========================================
# Logging Configuration
# ========================================
# Development: true (logs all SQL queries)
# Production: false (disable for performance)
LOG_SQL_QUERIES=false