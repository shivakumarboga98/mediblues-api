# Production Environment Variables Template
# Copy this to mediblues-api/.env.production and fill in your AWS values

# ========================================
# CRITICAL: Update all values below!
# DO NOT COMMIT THIS FILE TO GIT
# ========================================

# Node Environment
NODE_ENV=production

# ========================================
# FRONTEND DOMAIN (for CORS)
# Replace with your actual Amplify domain
# Format: https://your-app.amplifyapp.com
# ========================================
FRONTEND_URL=https://REPLACE_WITH_YOUR_AMPLIFY_DOMAIN.amplifyapp.com

# ========================================
# AWS CONFIGURATION
# ========================================
AWS_REGION=eu-north-1
S3_BUCKET_NAME=mediblues-assets

# Optional CloudFront Domain (leave empty if not using CDN)
# Format: d123456.cloudfront.net (without https://)
CLOUDFRONT_DOMAIN=

# ========================================
# RDS MYSQL DATABASE
# Get endpoint from: AWS RDS Console > Databases > your-db-instance
# Copy the "Endpoint" field (without port)
# Example: mediblues-mysql.c9akciq32.eu-north-1.rds.amazonaws.com
# ========================================
DB_HOST=REPLACE_WITH_YOUR_RDS_ENDPOINT.eu-north-1.rds.amazonaws.com
DB_PORT=3306
DB_NAME=mediblues_db
DB_USER=mediblues_user
DB_PASSWORD=REPLACE_WITH_YOUR_DATABASE_PASSWORD

# Database Connection Pool (optimized for Lambda)
DB_POOL_MIN=1
DB_POOL_MAX=2
DB_WAIT_FOR_CONNECTIONS=true
DB_CONNECTION_LIMIT=2
DB_QUEUE_LIMIT=0

# ========================================
# JWT CONFIGURATION
# CRITICAL: Change JWT_SECRET!
# Generate with: openssl rand -base64 32
# Or use any strong random 32+ character string
# ========================================
JWT_SECRET=REPLACE_WITH_YOUR_RANDOM_SECRET_KEY_MINIMUM_32_CHARACTERS

# Admin credentials (change password after first login!)
ADMIN_EMAIL=admin@mediblues.com
ADMIN_PASSWORD=Admin@123

# ========================================
# SERVERLESS FRAMEWORK
# ========================================
STAGE=prod

# ========================================
# OPTIONAL: Logging Configuration
# ========================================
LOG_SQL_QUERIES=false
LOG_LEVEL=info
